import AppShell from '@/Components/layout/AppShell'
import { Head, Link } from '@inertiajs/react'

export default function InventoryMovementShow({ movement }) {
    const quantity = Number(movement.quantity)

    return <AppShell title="Detalle de movimiento"><Head title="Detalle de movimiento" /><div className="mx-auto max-w-2xl space-y-6"><Link href="/almacen/movimientos" className="text-sm font-semibold text-indigo-600 hover:underline dark:text-indigo-300">← Volver a movimientos</Link><div className="rounded-2xl border border-slate-200 bg-white p-6 dark:border-neutral-800 dark:bg-neutral-900"><h1 className="text-2xl font-bold dark:text-white">{movement.type}</h1><dl className="mt-5 grid gap-4 sm:grid-cols-2"><div><dt className="text-xs uppercase text-slate-500">Producto</dt><dd className="mt-1 dark:text-white">{movement.product?.name} · {movement.product?.sku}</dd></div><div><dt className="text-xs uppercase text-slate-500">Ubicación</dt><dd className="mt-1 dark:text-white">{movement.location?.code}</dd></div><div><dt className="text-xs uppercase text-slate-500">Cantidad</dt><dd className={`mt-1 font-bold ${quantity >= 0 ? 'text-emerald-700' : 'text-rose-700'}`}>{quantity >= 0 ? '+' : ''}{movement.quantity}</dd></div><div><dt className="text-xs uppercase text-slate-500">Fecha</dt><dd className="mt-1 dark:text-white">{movement.occurred_at}</dd></div><div><dt className="text-xs uppercase text-slate-500">Referencia</dt><dd className="mt-1 dark:text-white">{movement.reference || '—'}</dd></div><div><dt className="text-xs uppercase text-slate-500">Usuario</dt><dd className="mt-1 dark:text-white">{movement.created_by?.name || '—'}</dd></div><div className="sm:col-span-2"><dt className="text-xs uppercase text-slate-500">Notas</dt><dd className="mt-1 whitespace-pre-wrap dark:text-slate-300">{movement.notes || '—'}</dd></div></dl></div></div></AppShell>
}
