import AppShell from '@/Components/layout/AppShell'
import { Head, Link } from '@inertiajs/react'

export default function InventoryLocationShow({ location }) {
    return <AppShell title="Detalle de ubicación"><Head title={location.code} /><div className="mx-auto max-w-4xl space-y-6"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><Link href="/almacen/ubicaciones" className="text-sm font-semibold text-indigo-600 hover:underline dark:text-indigo-300">← Volver a ubicaciones</Link><h1 className="mt-3 text-3xl font-bold text-slate-900 dark:text-white">{location.code}</h1></div><Link href={`/almacen/ubicaciones/${location.id}/editar`} className="rounded-xl bg-indigo-600 px-4 py-2.5 text-center font-semibold text-white">Editar</Link></div><div className="grid gap-4 rounded-2xl border border-slate-200 bg-white p-6 sm:grid-cols-2 dark:border-neutral-800 dark:bg-neutral-900"><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Nombre</dt><dd className="mt-1 dark:text-white">{location.name || '—'}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Estado</dt><dd className="mt-1 dark:text-white">{location.is_active ? 'Activa' : 'Inactiva'}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Orden</dt><dd className="mt-1 dark:text-white">{location.sort_order ?? '—'}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Productos</dt><dd className="mt-1 dark:text-white">{location.products?.length || 0}</dd></div><div className="sm:col-span-2"><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Descripción</dt><dd className="mt-1 whitespace-pre-wrap dark:text-slate-300">{location.description || '—'}</dd></div></div><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white dark:border-neutral-800 dark:bg-neutral-900"><div className="border-b border-slate-200 px-5 py-4 font-bold dark:border-neutral-800">Productos asignados</div><div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200 text-sm dark:divide-neutral-800"><thead className="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500 dark:bg-neutral-950"><tr><th className="px-5 py-3">Nombre</th><th className="px-5 py-3">SKU</th><th className="px-5 py-3">Código de barras</th><th className="px-5 py-3">Estado</th></tr></thead><tbody className="divide-y divide-slate-100 dark:divide-neutral-800">{(location.products || []).length === 0 ? <tr><td colSpan="4" className="px-5 py-8 text-center text-slate-500">No hay productos asignados.</td></tr> : location.products.map((product) => <tr key={product.id}><td className="px-5 py-3"><Link href={`/almacen/productos/${product.id}`} className="font-semibold text-indigo-600 hover:underline dark:text-indigo-300">{product.name}</Link></td><td className="px-5 py-3 font-mono text-xs">{product.sku}</td><td className="px-5 py-3 font-mono text-xs">{product.barcode || '—'}</td><td className="px-5 py-3">{product.is_active ? 'Activo' : 'Inactivo'}</td></tr>)}</tbody></table></div></div></div></AppShell>
}
