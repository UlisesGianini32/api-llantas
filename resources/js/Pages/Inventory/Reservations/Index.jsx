import AppShell from '@/Components/layout/AppShell'
import { Head, Link, router, usePage } from '@inertiajs/react'
import { useState } from 'react'

export default function InventoryReservationsIndex({ reservations, filters, statuses }) {
    const { flash = {} } = usePage().props
    const [search, setSearch] = useState(filters?.search || '')
    const [status, setStatus] = useState(filters?.status || '')
    const submitSearch = (event) => {
        event.preventDefault()
        router.get('/almacen/reservas', { search, status }, { preserveState: true, replace: true })
    }
    const action = (name, id) => router.post('/almacen/reservas/' + id + '/' + name, {}, { preserveScroll: true })

    return <AppShell title="Reservas"><Head title="Reservas de inventario" /><div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-indigo-600 dark:text-indigo-300">Almacén</p><h1 className="mt-2 text-3xl font-bold text-slate-900 dark:text-white">Reservas</h1><p className="mt-2 text-sm text-slate-500 dark:text-slate-400">Unidades apartadas sin modificar el stock físico.</p></div><Link href="/almacen/reservas/crear" className="inline-flex items-center justify-center rounded-xl bg-indigo-600 px-4 py-2.5 font-semibold text-white hover:bg-indigo-700">Crear reserva</Link></div>{flash.success && <div className="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-semibold text-emerald-800">{flash.success}</div>}<form onSubmit={submitSearch} className="flex flex-col gap-3 rounded-2xl border border-slate-200 bg-white p-4 sm:flex-row dark:border-neutral-800 dark:bg-neutral-900"><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Buscar producto, SKU, barcode, ubicación o referencia" className="min-w-0 flex-1 rounded-xl border border-slate-300 px-4 py-2.5 text-sm dark:border-neutral-700 dark:bg-neutral-950 dark:text-white" /><select value={status} onChange={(event) => setStatus(event.target.value)} className="rounded-xl border border-slate-300 px-4 py-2.5 text-sm dark:border-neutral-700 dark:bg-neutral-950 dark:text-white"><option value="">Todos los estados</option>{statuses.map((item) => <option key={item} value={item}>{item}</option>)}</select><button type="submit" className="rounded-xl bg-slate-900 px-5 py-2.5 text-sm font-semibold text-white dark:bg-white dark:text-slate-900">Buscar</button></form><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-neutral-800 dark:bg-neutral-900"><div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200 text-sm dark:divide-neutral-800"><thead className="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500 dark:bg-neutral-950 dark:text-slate-400"><tr><th className="px-5 py-4">Fecha</th><th className="px-5 py-4">Producto / SKU</th><th className="px-5 py-4">Ubicación</th><th className="px-5 py-4">Cantidad</th><th className="px-5 py-4">Estado</th><th className="px-5 py-4">Fuente / referencia</th><th className="px-5 py-4">Expiración</th><th className="px-5 py-4">Usuario</th><th className="px-5 py-4">Acciones</th></tr></thead><tbody className="divide-y divide-slate-100 dark:divide-neutral-800">{reservations.data.length === 0 ? <tr><td colSpan="9" className="px-5 py-12 text-center text-slate-500">No hay reservas para mostrar.</td></tr> : reservations.data.map((reservation) => <tr key={reservation.id}><td className="whitespace-nowrap px-5 py-4">{reservation.created_at}</td><td className="px-5 py-4"><Link href={'/almacen/productos/' + reservation.product?.id} className="font-semibold text-indigo-600 hover:underline dark:text-indigo-300">{reservation.product?.name}</Link><div className="font-mono text-xs text-slate-500">{reservation.product?.sku}</div></td><td className="px-5 py-4">{reservation.location?.code || 'Global'}</td><td className="px-5 py-4 font-bold">{reservation.quantity}</td><td className="px-5 py-4">{reservation.status}</td><td className="px-5 py-4">{reservation.source_type || '—'} / {reservation.reference || '—'}</td><td className="px-5 py-4">{reservation.expires_at || '—'}</td><td className="px-5 py-4">{reservation.created_by?.name || '—'}</td><td className="px-5 py-4"><div className="flex flex-wrap gap-2"><Link href={'/almacen/reservas/' + reservation.id} className="text-indigo-600 hover:underline dark:text-indigo-300">Ver</Link>{reservation.status === 'ACTIVE' && <><button type="button" onClick={() => action('liberar', reservation.id)} className="text-amber-700 hover:underline">Liberar</button><button type="button" onClick={() => action('cancelar', reservation.id)} className="text-rose-700 hover:underline">Cancelar</button><button type="button" onClick={() => action('cumplir', reservation.id)} className="text-emerald-700 hover:underline">Cumplir</button></>}</div></td></tr>)}</tbody></table></div></div></div></AppShell>
}
