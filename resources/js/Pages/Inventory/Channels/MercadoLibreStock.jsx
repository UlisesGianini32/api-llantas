import AppShell from '@/Components/layout/AppShell'
import { Head, router, usePage } from '@inertiajs/react'
import { useState } from 'react'

const labels = { READY: 'Listo', SUCCESS: 'Correcto', SKIPPED_SYNC_DISABLED: 'Sync desactivada', SKIPPED_LINK_INACTIVE: 'Vínculo inactivo', SKIPPED_PRODUCT_INACTIVE: 'Producto inactivo', INVALID_ACCOUNT: 'Cuenta inválida', INVALID_EXTERNAL_ID: 'Publicación inválida', UNSUPPORTED: 'No soportado', LOCKED: 'En curso' }

export default function MercadoLibreStock({ preview = { rows: [], counts: {} }, accounts = [], statuses = [], canManage = false }) {
    const { flash } = usePage().props
    const [search, setSearch] = useState(preview.filters?.search || '')
    const [account, setAccount] = useState(preview.filters?.account_key || '')
    const [result, setResult] = useState(preview.filters?.result || '')
    const [enabled, setEnabled] = useState(preview.filters?.enabled || '')
    const analyze = (event) => { event.preventDefault(); router.get('/almacen/canales/mercado-libre/stock', { analyze: 1, search, account_key: account, result, enabled }, { preserveState: true, replace: true }) }
    const sync = (id) => { if (window.confirm('Solo se actualizarán vínculos con sincronización habilitada. ¿Continuar?')) router.post('/almacen/canales/mercado-libre/stock/sync', { link_id: id }, { preserveScroll: true }) }
    return <AppShell title="Stock Mercado Libre"><Head title="Stock Mercado Libre" /><div className="space-y-6"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-indigo-600">Almacén</p><h1 className="mt-2 text-3xl font-bold dark:text-white">Stock Mercado Libre</h1><p className="mt-2 text-sm text-slate-500">Preview por defecto. Solo los vínculos activos con sincronización habilitada pueden escribir en Mercado Libre.</p></div>{flash.success && <div className="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-semibold text-emerald-800">{flash.success}</div>}{flash.error && <div className="rounded-xl border border-red-200 bg-red-50 p-4 text-sm font-semibold text-red-800">{flash.error}</div>}<form onSubmit={analyze} className="flex flex-col gap-3 rounded-2xl border bg-white p-4 sm:flex-row dark:border-neutral-800 dark:bg-neutral-900"><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Buscar SKU o MLM" className="min-w-0 flex-1 rounded-xl border px-4 py-2.5 text-sm dark:border-neutral-700 dark:bg-neutral-950 dark:text-white" /><select value={account} onChange={(e) => setAccount(e.target.value)} className="rounded-xl border px-4 py-2.5 text-sm dark:border-neutral-700 dark:bg-neutral-950 dark:text-white"><option value="">Todas las cuentas</option>{accounts.map((item) => <option key={item.id} value={item.id}>{item.nickname || item.meli_user_id} ({item.id})</option>)}</select><select value={result} onChange={(e) => setResult(e.target.value)} className="rounded-xl border px-4 py-2.5 text-sm dark:border-neutral-700 dark:bg-neutral-950 dark:text-white"><option value="">Todos los estados</option>{statuses.map((item) => <option key={item} value={item}>{labels[item] || item}</option>)}</select><select value={enabled} onChange={(e) => setEnabled(e.target.value)} className="rounded-xl border px-4 py-2.5 text-sm dark:border-neutral-700 dark:bg-neutral-950 dark:text-white"><option value="">Sync habilitada: todas</option><option value="1">Sync habilitada</option><option value="0">Sync deshabilitada</option></select><button className="rounded-xl bg-slate-900 px-5 py-2.5 text-sm font-semibold text-white">Analizar</button></form><div className="grid gap-3 sm:grid-cols-4">{Object.entries(preview.counts || {}).map(([key, count]) => <div key={key} className="rounded-2xl border bg-white p-4 dark:border-neutral-800 dark:bg-neutral-900"><div className="text-xs uppercase text-slate-500">{labels[key] || key}</div><div className="mt-1 text-2xl font-bold dark:text-white">{count}</div></div>)}</div><div className="overflow-hidden rounded-2xl border bg-white dark:border-neutral-800 dark:bg-neutral-900"><div className="overflow-x-auto"><table className="min-w-full divide-y text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500 dark:bg-neutral-950"><tr><th className="px-4 py-3">SKU / producto</th><th className="px-4 py-3">Cuenta / MLM</th><th className="px-4 py-3">Stock</th><th className="px-4 py-3">Estado</th><th className="px-4 py-3">Acción</th></tr></thead><tbody className="divide-y">{preview.rows.map((row) => <tr key={row.id}><td className="px-4 py-3"><div className="font-semibold">{row.sku || '—'}</div><div className="text-xs text-slate-500">{row.product_name || '—'} · {row.product_type || '—'}</div></td><td className="px-4 py-3"><div>{row.account_name || row.account_key || '—'}</div><div className="font-mono text-xs">{row.external_listing_id}{row.external_variant_id ? ` · ${row.external_variant_id}` : ''}</div></td><td className="px-4 py-3">F {row.physical} · R {row.reserved} · <strong>{row.target}</strong></td><td className="px-4 py-3">{labels[row.status] || row.status}<div className="text-xs text-slate-500">{row.reason || ''}</div></td><td className="px-4 py-3">{canManage && row.status === 'READY' && <button type="button" onClick={() => sync(row.id)} className="text-indigo-600 hover:underline">Sincronizar</button>}</td></tr>)}</tbody></table></div></div></div></AppShell>
}
