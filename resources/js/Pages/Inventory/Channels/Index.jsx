import AppShell from '@/Components/layout/AppShell'
import { Head, Link, router, usePage } from '@inertiajs/react'
import { useState } from 'react'

const labels = { mercado_libre: 'Mercado Libre', amazon: 'Amazon', shopify: 'Shopify' }

export default function InventoryChannelLinksIndex({ links, filters = {}, channels = [], canManage = false }) {
    const { flash } = usePage().props
    const [search, setSearch] = useState(filters.search || '')
    const [channel, setChannel] = useState(filters.channel || '')
    const [active, setActive] = useState(filters.active || '')
    const submit = (event) => { event.preventDefault(); router.get('/almacen/canales', { search, channel, active }, { preserveState: true, replace: true }) }
    const toggle = (id) => router.patch(`/almacen/canales/${id}/estado`, {}, { preserveScroll: true })
    return <AppShell title="Enlaces de canales"><Head title="Enlaces de canales" /><div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-indigo-600">Almacén</p><h1 className="mt-2 text-3xl font-bold text-slate-900 dark:text-white">Enlaces de canales</h1><p className="mt-2 text-sm text-slate-500">Relaciones manuales entre productos internos y publicaciones externas.</p></div>{canManage && <Link href="/almacen/canales/crear" className="rounded-xl bg-indigo-600 px-4 py-2.5 text-center font-semibold text-white">Nuevo enlace</Link>}</div>{flash.success && <div className="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-semibold text-emerald-800">{flash.success}</div>}<form onSubmit={submit} className="flex flex-col gap-3 rounded-2xl border border-slate-200 bg-white p-4 sm:flex-row dark:border-neutral-800 dark:bg-neutral-900"><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Buscar SKU, nombre, barcode o ID externo" className="min-w-0 flex-1 rounded-xl border px-4 py-2.5 text-sm dark:border-neutral-700 dark:bg-neutral-950 dark:text-white" /><select value={channel} onChange={(e) => setChannel(e.target.value)} className="rounded-xl border px-4 py-2.5 text-sm dark:border-neutral-700 dark:bg-neutral-950 dark:text-white"><option value="">Todos los canales</option>{channels.map((item) => <option key={item} value={item}>{labels[item] || item}</option>)}</select><select value={active} onChange={(e) => setActive(e.target.value)} className="rounded-xl border px-4 py-2.5 text-sm dark:border-neutral-700 dark:bg-neutral-950 dark:text-white"><option value="">Todos los estados</option><option value="1">Activos</option><option value="0">Inactivos</option></select><button type="submit" className="rounded-xl bg-slate-900 px-5 py-2.5 text-sm font-semibold text-white">Buscar</button></form><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white dark:border-neutral-800 dark:bg-neutral-900"><div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200 text-sm dark:divide-neutral-800"><thead className="bg-slate-50 text-left text-xs font-semibold uppercase text-slate-500 dark:bg-neutral-950"><tr><th className="px-5 py-4">Producto</th><th className="px-5 py-4">Canal</th><th className="px-5 py-4">Cuenta</th><th className="px-5 py-4">Identificador</th><th className="px-5 py-4">Estado remoto</th><th className="px-5 py-4">Activo</th><th className="px-5 py-4">Acciones</th></tr></thead><tbody className="divide-y divide-slate-100 dark:divide-neutral-800">{links.data.length === 0 ? <tr><td colSpan="7" className="px-5 py-12 text-center text-slate-500">No hay enlaces para mostrar.</td></tr> : links.data.map((link) => <tr key={link.id}><td className="px-5 py-4"><div className="font-semibold">{link.product?.name}</div><div className="font-mono text-xs text-slate-500">{link.product?.sku}</div></td><td className="px-5 py-4">{labels[link.channel] || link.channel}</td><td className="px-5 py-4">{link.account_key || '—'}</td><td className="px-5 py-4 font-mono">{link.external_listing_id || link.external_product_id || link.external_variant_id || '—'}</td><td className="px-5 py-4">{link.remote_status || '—'}</td><td className="px-5 py-4">{link.is_active ? 'Activo' : 'Inactivo'}</td><td className="px-5 py-4"><div className="flex flex-wrap gap-3"><Link href={`/almacen/canales/${link.id}`} className="text-indigo-600 hover:underline">Ver</Link>{canManage && <><Link href={`/almacen/canales/${link.id}/editar`} className="text-indigo-600 hover:underline">Editar</Link><button type="button" onClick={() => toggle(link.id)} className="text-amber-700 hover:underline">{link.is_active ? 'Desactivar' : 'Activar'}</button></>}</div></td></tr>)}</tbody></table></div></div></div></AppShell>
}
